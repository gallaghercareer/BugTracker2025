trigger:
- main

pool:
  name: 'Default'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

stages:
# ────────────────────────── Build & Deploy to Dev Slot ──────────────────────────
- stage: BuildAndDeploy
  displayName: 'Build & Deploy to Dev'
  jobs:
  # -------- Build job --------
  - job: Build
    displayName: 'Build & Test'
    steps:
    # Restore
    - task: NuGetToolInstaller@1
    - task: NuGetCommand@2
      inputs:
        restoreSolution: '$(solution)'

    # Build **and publish to a file-system folder**
    - task: VSBuild@1
      inputs:
        solution: '$(solution)'
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'
        msbuildArgs: >
          /p:DeployOnBuild=true
          /p:WebPublishMethod=FileSystem          # ⬅️ MSDeploy package removed
          /p:PublishProtocol=FileSystem
          /p:publishUrl=$(Build.ArtifactStagingDirectory)/publish

    # Unit tests (unchanged)
    - task: VSTest@2
      inputs:
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'

    # Zip the published output so Zip-Deploy can mount it
    - task: ArchiveFiles@2
      displayName: 'Archive publish folder to WebApp.zip'
      inputs:
        rootFolderOrFile: '$(Build.ArtifactStagingDirectory)/publish'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/WebApp.zip'
        replaceExistingArchive: true

    # Publish the ZIP as the pipeline artifact (same name/path as before)
    - task: PublishPipelineArtifact@1
      displayName: 'Publish WebApp.zip artifact'
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)/WebApp.zip'
        artifact: 'drop'

  # -------- Deploy job --------
  - job: DeployToDev
    displayName: 'Deploy to Dev Slot'
    dependsOn: Build
    steps:
    - download: current
      artifact: drop

    - task: AzureWebApp@1
      displayName: 'Deploy to Dev Slot'
      inputs:
        azureSubscription: 'azuredevops-to-azureresource-service-connection'
        appType: 'webApp'
        appName: 'BugTrackerApi20250516235431'
        deployToSlotOrASE: true
        resourceGroupName: 'FinalProReRe20250306152958ResourceGroup'
        slotName: 'dev'
        package: '$(Pipeline.Workspace)/drop/WebApp.zip'
        deploymentMethod: 'runFromPackage'        # ⬅️ zip-deploy / run-from-package

# ────────────────────────── Swap to Production (Approval Required) ──────────────────────────
- stage: SwapToProd
  displayName: 'Swap Dev → Production'
  dependsOn: BuildAndDeploy
  jobs:
  - deployment: SwapToProduction
    displayName: 'Swap to Production'
    environment: 'ProductionEnv'    # triggers pre-deployment approval
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureAppServiceManage@0
            displayName: 'Swap Slots'
            inputs:
              azureSubscription: 'azuredevops-to-azureresource-service-connection'
              action: 'Swap Slots'
              WebAppName: 'BugTrackerApi20250516235431'
              ResourceGroupName: 'FinalProReRe20250306152958ResourceGroup'
              SourceSlot: 'dev'
              SwapWithProduction: true
